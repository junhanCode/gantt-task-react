# TitleCell 自定义任务名列组件

## 概述

TitleCell 是一个功能丰富的任务名列自定义渲染组件，用于在甘特图的任务列表中展示任务的详细信息和状态标记。

## 功能特性

### 1. 基础信息显示
- **未读标记**: 红色 `*` 标记表示未读任务
- **展开/折叠图标**: 有子任务时显示展开/折叠控制
- **任务编号**: 显示任务的序号
- **项目标签**: 显示任务所属的项目（如【系統開發】），支持多项目标签

### 2. 状态标记
- **关注标记** ⭐: 黄色星星图标表示已关注的任务
- **隐藏标记** 👁️: 灰色眼睛图标表示已隐藏的任务
- **跟进标记** ⚠️: 红色警告图标表示需要跟进的任务
- **会议决议**: 特殊的会议决议任务标记

### 3. 时间状态
- **延期标记**: 显示延期天数（粉色背景）
- **暂停标记**: 显示暂停天数（灰色背景）

## 文件结构

```
example/src/components/TitleCell/
├── TitleCell.tsx          # 组件主文件
├── TitleCell.module.css   # 样式文件
└── index.ts               # 导出文件
```

## 使用方法

### 1. 在 Demo 中启用

在界面上找到"新功能展示说明"区域，勾选"启用自定义任务名列"复选框即可启用 TitleCell 渲染。

### 2. 组件集成

```tsx
import TitleCell from './components/TitleCell';

// 在 columnRenderers 中使用
columnRenderers={{
  name: (task: Task) => {
    const record = {
      ...(task as any),
      id: task.id,
    };
    return (
      <TitleCell
        value={task.name}
        record={record}
        expandedRowKeys={expandedTaskKeys}
        onRead={handleTaskRead}
        onAdd={handleTaskAdd}
        onCheck={handleTaskCheck}
        onExpand={handleTaskExpand}
      />
    );
  }
}}
```

## Props 说明

| 属性 | 类型 | 必填 | 说明 |
|------|------|------|------|
| value | string | 是 | 任务名称 |
| record | any | 是 | 任务记录对象 |
| expandedRowKeys | any[] | 是 | 已展开的任务ID数组 |
| onRead | (record: any) => void | 否 | 标记已读的回调 |
| onAdd | (taskID: number) => void | 否 | 新增/编辑任务的回调 |
| onCheck | (record: any, operate: string) => void | 否 | 查看/操作任务的回调 |
| onExpand | (expanded: boolean, record: any) => void | 是 | 展开/折叠子行的回调 |

## Record 对象字段

TitleCell 组件需要 record 对象包含以下字段：

```typescript
{
  id: string;                    // 任务ID
  name: string;                  // 任务名称
  read?: boolean;                // 是否已读
  focus?: boolean;               // 是否关注
  hidden?: boolean;              // 是否隐藏
  follow?: boolean;              // 是否跟进
  delayDays?: number;            // 延期天数
  suspend?: boolean;             // 是否暂停
  suspendDays?: number;          // 暂停天数
  hasChildren?: boolean;         // 是否有子任务
  layer?: number;                // 层级（1为父任务，2为子任务）
  number?: number;               // 任务编号
  parentId?: number;             // 父任务ID
  projectTags?: string[];        // 项目标签数组
  category?: string;             // 任务分类（"6-5"表示会议决议）
  statusInfoVo?: {               // 状态信息
    description: string;
    color: string;
  };
}
```

## 样式定制

可以通过修改 `TitleCell.module.css` 文件来定制样式：

```css
.titleCell { /* 主容器 */ }
.jump_Ctn { /* 点击区域 */ }
.unRead { /* 未读标记 */ }
.expandIcon { /* 展开/折叠图标 */ }
.row_index { /* 任务编号 */ }
.title { /* 任务标题 */ }
.statusCtn { /* 状态标签容器 */ }
```

## 注意事项

1. **依赖**:
   - antd 5.x（Tooltip, Flex 组件）
   - @ant-design/icons（图标）
   
2. **性能**:
   - 组件已使用 React.memo 优化
   - 建议在大量任务时进行虚拟滚动

3. **扩展性**:
   - 可以根据需要添加更多的状态标记
   - 可以自定义项目标签的显示逻辑
   - 支持自定义样式主题

## 示例截图

启用 TitleCell 后，任务列表会显示如下效果：

```
[*] > 1 【系統開發】任务标题 ⭐ ⚠️ [延期 5 天]
    > 2 【RFQ】子任务1 👁️
    > 3 【Others】子任务2 [暂停 3 天]
```

其中：
- `[*]` = 未读标记
- `>` = 展开/折叠图标
- `1,2,3` = 任务编号
- `【系統開發】` = 项目标签
- ⭐ = 关注标记
- ⚠️ = 跟进标记
- 👁️ = 隐藏标记
- `[延期 N 天]` = 延期标记
- `[暂停 N 天]` = 暂停标记
